# DirtyDuties Vue

Migración de la app estática HTML/CSS a Vue 3 + Vite + Vue Router.

## Ejecutar

```bash
npm install
npm run dev
```

## Rutas

- `/` landing
- `/login` login
- `/formulario` formulario
- `/principal` panel principal
